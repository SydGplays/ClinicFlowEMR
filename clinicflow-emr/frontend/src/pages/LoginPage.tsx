import { useState } from 'react'; import { Activity, ArrowRight, LockKeyhole, ShieldCheck } from 'lucide-react'; import { Navigate } from 'react-router-dom'; import { useAuth } from '../context/AuthContext'; import { ErrorAlert } from '../components/UI'; import { getError } from '../services/api';
export default function LoginPage(){const {user,login}=useAuth();const [email,setEmail]=useState('admin@clinicflow.demo'),[password,setPassword]=useState('Demo123!'),[error,setError]=useState(''),[busy,setBusy]=useState(false);if(user)return <Navigate to="/"/>;async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setError('');try{await login(email,password)}catch(e){setError(getError(e))}finally{setBusy(false)}}return <div className="grid min-h-screen lg:grid-cols-2"><section className="hidden bg-[#0e3440] p-16 text-white lg:flex lg:flex-col lg:justify-between"><div className="flex items-center gap-3 text-xl font-bold"><span className="rounded-xl bg-brand-500 p-2"><Activity/></span> ClinicFlow EMR</div><div><p className="mb-5 text-xs font-semibold uppercase tracking-[.25em] text-brand-500">Portfolio demonstration</p><h1 className="max-w-xl text-5xl font-bold leading-tight">Clinical clarity,<br/>from check-in to care.</h1><p className="mt-6 max-w-lg text-lg leading-8 text-slate-300">A focused electronic medical records experience for modern care teams—built entirely with fictional data.</p></div><div className="flex items-center gap-2 text-sm text-slate-400"><ShieldCheck size={18}/> No real patient information is stored in this demo.</div></section><section className="flex items-center justify-center p-6"><form className="w-full max-w-md" onSubmit={submit}><div className="mb-9 lg:hidden"><div className="flex items-center gap-2 text-xl font-bold text-ink"><Activity className="text-brand-600"/> ClinicFlow EMR</div></div><p className="text-sm font-semibold text-brand-600">Welcome back</p><h2 className="mt-2 text-3xl font-bold text-ink">Sign in to your workspace</h2><p className="mb-8 mt-2 text-sm text-slate-500">Use the pre-filled portfolio demo credentials.</p><ErrorAlert message={error}/><label className="label">Email address</label><input className="input mb-5" type="email" value={email} onChange={e=>setEmail(e.target.value)} required/><label className="label">Password</label><div className="relative mb-6"><LockKeyhole className="absolute left-3.5 top-3 text-slate-400" size={17}/><input className="input pl-10" type="password" value={password} onChange={e=>setPassword(e.target.value)} required/></div><button className="btn-primary w-full" disabled={busy}>{busy?'Signing in…':'Sign in'}<ArrowRight size={17}/></button><div className="mt-6 rounded-xl bg-slate-100 p-4 text-xs leading-5 text-slate-500"><strong className="text-slate-700">Demo only.</strong> All names, contacts, visits, and clinical content are fictional.</div></form></section></div>}

